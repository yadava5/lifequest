# LifeQuest Legacy Prototype

This folder contains the original CRA + Express + Electron prototype. See `docs/legacy.md` for full setup instructions.

Quick start:

```bash
npm install
npm run dev
```

For the Electron shell:

```bash
npm run desktop
```
